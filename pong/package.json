{
  "name": "pong",
  "version": "1.0.0",
  "description": "A canvas version of the classic game Pong",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server --files 'index.html, index.js'",
    "watch:scripts": "rollup --config --watch",
    "start": "npm-run-all --parallel watch:scripts serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.6",
    "npm-run-all": "^4.1.3",
    "rollup": "^0.63.2"
  }
}
